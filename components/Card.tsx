import { ReactNode } from "react"; import cn from "classnames"; export function Card({title,subtitle,children,className}:{title?:string;subtitle?:string;children:ReactNode;className?:string}){return (<section className={cn("card",className)}>{title&&<h2 className="text-lg font-semibold">{title}</h2>}{subtitle&&<p className="mt-1 text-sm text-stoney-600">{subtitle}</p>}<div className="mt-4">{children}</div></section>);}
